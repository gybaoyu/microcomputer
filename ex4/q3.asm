DATA SEGMENT
X DW 100
Y DW -30
Z DW 50
W DW ?
DATA ENDS

CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START:
    MOV AX, DATA
    MOV DS, AX

    MOV AX, X
    ADD AX, Y
    ADD AX, 24
    SUB AX, Z
    MOV W, AX

    MOV AH, 4CH
    INT 21H
CODE ENDS
END START

